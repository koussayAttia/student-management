pipeline {
    agent any

    tools {
        jdk 'JAVA_Home'
        maven 'M2_HOME'
    }

    environment {
        GIT_CREDENTIALS_ID       = '14409463'
        GIT_BRANCH               = 'main'
        GIT_URL                  = 'https://github.com/koussayAttia/student-management.git'

        // Doit correspondre exactement à ton repo Docker Hub
        // Namespace = ton username Docker Hub : koussaybenattia
        DOCKERHUB_REPO           = 'koussaybenattia/student-management'
        DOCKERHUB_CREDENTIALS_ID = '03373df4-ddd4-4b2f-bae3-f0f1748eb56a'
    }

    stages {

        stage('Checkout') {
            steps {
                echo "Récupération du projet depuis GitHub (dépôt privé)..."
                git branch: env.GIT_BRANCH,
                    url: env.GIT_URL,
                    credentialsId: env.GIT_CREDENTIALS_ID
            }
        }

        stage('Build (clean & package, sans tests)') {
            steps {
                echo "Build Maven avec skip des tests..."
                dir('student-management') {
                    sh 'mvn clean package -DskipTests'
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                echo "Build de l'image Docker..."
                dir('student-management') {
                    sh 'docker build -t $DOCKERHUB_REPO:$BUILD_NUMBER .'
                }
            }
        }

        stage('Push Docker Image to Docker Hub') {
            steps {
                echo "Push de l'image Docker sur Docker Hub..."
                withCredentials([
                    usernamePassword(
                        credentialsId: env.DOCKERHUB_CREDENTIALS_ID,
                        usernameVariable: 'DOCKERHUB_USER',
                        passwordVariable: 'DOCKERHUB_PASS'
                    )
                ]) {
                    sh '''
                        echo "Login à Docker Hub..."
                        echo "$DOCKERHUB_PASS" | docker login -u "$DOCKERHUB_USER" --password-stdin

                        echo "Push de l'image..."
                        docker push $DOCKERHUB_REPO:$BUILD_NUMBER
                    '''
                }
            }
        }
    }

    post {
        success {
            echo 'Build réussi 🎉'
        }
        failure {
            echo 'Build échoué ❌'
        }
    }
}

