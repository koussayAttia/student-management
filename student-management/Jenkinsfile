pipeline {
    agent any

    tools {
        jdk 'JAVA_HOME'
        maven 'M2_HOME'
    }

    environment {
        GIT_CREDENTIALS_ID       = '14409463'
        GIT_BRANCH               = 'main'
        GIT_URL                  = 'https://github.com/koussayAttia/student-management.git'

        DOCKERHUB_REPO           = 'koussaybenattia/student-management'
        DOCKERHUB_CREDENTIALS_ID = 'efb9d575-c792-451d-8397-39d6121e21ba'
    }

    stages {

        stage('Checkout') {
            steps {
                echo "Récupération du projet depuis GitHub (dépôt privé)..."
                git branch: env.GIT_BRANCH,
                    url: env.GIT_URL,
                    credentialsId: env.GIT_CREDENTIALS_ID
            }
        }

        stage('Build (clean & package, sans tests)') {
            steps {
                echo "Build Maven avec skip des tests..."
                dir('student-management') {
                    sh 'mvn clean package -DskipTests'
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                echo "Build de l'image Docker..."
                dir('student-management') {
                    sh 'docker build -t $DOCKERHUB_REPO:$BUILD_NUMBER .'
                }
            }
        }

        stage('Push Docker Image to Docker Hub') {
            steps {
                echo "Push de l'image Docker sur Docker Hub..."
                withCredentials([
                    usernamePassword(
                        credentialsId: env.DOCKERHUB_CREDENTIALS_ID,
                        usernameVariable: 'DOCKERHUB_USER',
                        passwordVariable: 'DOCKERHUB_PASS'
                    )
                ]) {
                    sh '''
                        echo "Login à Docker Hub..."
                        echo "$DOCKERHUB_PASS" | docker login -u "$DOCKERHUB_USER" --password-stdin

                        echo "Push de l'image..."
                        docker push $DOCKERHUB_REPO:$BUILD_NUMBER
                    '''
                }
            }
        }

        stage('Deploy to Kubernetes') {
            steps {
                echo "Déploiement sur Kubernetes (namespace devops)..."
                dir('student-management') {
                    sh '''
                        echo "Application des manifests Kubernetes..."
        
                        # Démarrage de Minikube seulement si nécessaire
                        minikube status || minikube start --driver=docker --cpus=2 --memory=4096 --wait=all --wait-timeout=600s
        
                        echo "Création du namespace devops s'il n'existe pas..."
                        minikube kubectl -- get namespace devops || minikube kubectl -- create namespace devops
        
                        echo "Application des manifests Kubernetes..."
                        minikube kubectl -- apply -f mysql-deployment.yaml -n devops
                        minikube kubectl -- apply -f sonarqube-deployment.yaml -n devops
                        minikube kubectl -- apply -f spring-deployment.yaml -n devops
        
                        echo "Vérification des pods..."
                        minikube kubectl -- get pods -n devops
                    '''
                }
            }
        }
    }

    post {
        success {
            echo 'Build réussi 🎉'
        }
        failure {
            echo 'Build échoué ❌'
        }
    }
}
